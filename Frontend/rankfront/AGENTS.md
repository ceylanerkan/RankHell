# Codex Instructions

Before making any changes in this project, completely read and follow:

- `CLAUDE.md`

Treat all project architecture, design-system, component, scope, testing,
validation, and do-not-touch rules in `CLAUDE.md` as binding instructions
for Codex as well.

For frontend design-related work, also completely read and follow:

- `docs/DESIGN_SYSTEM.md`

Do not introduce new colors, button styles, component patterns, or visual
rules that conflict with the existing project instructions.

When `AGENTS.md` and `CLAUDE.md` conflict, follow `AGENTS.md`.
